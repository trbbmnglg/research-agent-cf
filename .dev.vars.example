# Bring-your-own-key (BYOK): this app reads the LLM + Tavily keys from each
# request (entered in the UI), used in-memory and never stored. No Worker
# secrets are required — you can ignore/delete .dev.vars entirely.
